# 智能物流管理系统 UI 设计方案

## 1. 设计定位

本系统是基于 EasyX 的桌面端物流管理系统，主要用于课程设计演示和业务流程验证。UI 设计应服务于“看得清、操作稳、流程顺”的目标，不追求复杂动效或商业化视觉包装。

整体风格延续现有界面：物流科技背景图 + 居中白色操作面板 + 蓝色边框 + 表格化信息展示。界面应保持朴素、统一、稳定，突出订单、库存、运输等业务数据。

## 2. 用户与场景

系统用户分为管理员、客服、仓储员、运输人员等角色，不同角色通过权限位掩码看到不同菜单。

核心使用场景：

- 管理员：用户管理、订单审核、系统配置、数据查看。
- 客服：创建订单、查询订单、跟踪订单状态。
- 仓储员：查看待出库订单、入库、出库、库存查询、库存盘点。
- 运输人员：调度运输、更新运输轨迹、处理异常状态。

UI 需要让用户快速定位当前模块、当前身份、当前操作对象，并能随时返回上级菜单。

## 3. 视觉规范

### 3.1 画布与背景

- 窗口尺寸固定为 `800 x 600`。
- 背景使用现有物流科技主题图片，作为全局视觉基底。
- 所有业务操作均放入白底面板中，避免文字直接压在复杂背景上。
- 背景只负责营造系统主题，不承担信息展示。

### 3.2 主面板

通用业务页面使用统一面板：

- 坐标：`UI_PANEL_X = 180`，`UI_PANEL_Y = 60`。
- 尺寸：`UI_PANEL_W = 440`，`UI_PANEL_H = 480`。
- 背景：纯白 `WHITE_COLOR`。
- 边框：浅蓝 `FRAME_BLUE`。
- 面板内保持 25px 到 35px 的左右安全边距。

特殊页面可使用独立面板：

- 启动页：居中白底框，建议 `320 x 250`。
- 登录页：居中白底框，宽度可略大于启动页，确保账号、密码、按钮不拥挤。
- 创建订单等长表单：可使用 `520 x 500` 的扩展面板。

### 3.3 色彩

沿用 `ui_config.h` 中的现有色彩：

- 主色：`PRIMARY = RGB(30,144,255)`，用于主按钮、选中状态、焦点边框。
- 正文：`TEXT_MAIN = RGB(44,62,80)`。
- 次要文字：`TEXT_MUTED = RGB(127,140,141)`。
- 表头：`BG_TABLE_HDR = RGB(230,235,240)`。
- 表格线：`GRAY_LINE = RGB(210,210,210)`。
- 输入框边框：`INPUT_BORDER = RGB(200,200,200)`。
- 危险操作：`RED_BTN = RGB(220,53,69)`，仅用于驳回、删除、异常等操作。

颜色使用原则：

- 蓝色表示可执行、当前焦点、普通确认。
- 红色表示不可逆或负向操作。
- 灰色表示弱提示、禁用、空状态。
- 不新增大面积渐变色，避免破坏现有界面一致性。

### 3.4 字体

统一使用黑体：

- 页面标题：20px，居中。
- 菜单按钮：16px。
- 表格文字：14px。
- 提示文字：14px，使用灰色。

标题不宜过大，当前系统是操作型工具，不采用宣传页式大标题。

## 4. 布局规范

### 4.1 页面结构

业务页面统一采用四段式结构：

1. 标题区：显示“智能物流管理系统 + 当前模块名称”。
2. 元信息区：显示当前用户、角色、登录时间。
3. 操作区：放置查询框、输入框、按钮或表单字段。
4. 数据区：放置表格、分页、空状态提示。

推荐纵向坐标：

- 标题：`UI_PANEL_Y + 18`。
- 用户信息：`UI_PANEL_Y + 62`。
- 查询栏：`UI_PANEL_Y + 84`。
- 表格：`UI_PANEL_Y + 122`。
- 分页提示：`UI_PANEL_Y + 240` 或面板底部。

### 4.2 菜单页

主菜单和子菜单采用居中网格或纵向列表：

- 主菜单：优先使用 2 列 x 4 行布局。
- 子菜单：功能少于 4 个时使用纵向按钮。
- 每个按钮宽度保持一致，不因文字长短变化。
- 当前焦点按钮使用蓝底白字。

菜单文字应直接说明业务动作，例如：

- 创建订单
- 订单审核
- 订单查询
- 订单跟踪
- 入库管理
- 出库管理
- 库存查询

避免在按钮中使用过长说明。

### 4.3 表单页

表单页采用左标签、右输入框布局：

- 标签右对齐或固定左边距。
- 输入框宽度统一，最少 220px。
- 长表单可压缩单行高度，但必须保留 8px 以上垂直呼吸空间。
- 提交按钮和返回按钮放在底部，水平分布。

订单创建页字段较多，建议使用扩展面板：

- 第一屏显示订单号、客户信息、货物信息、地址信息。
- 系统自动生成字段用普通文本展示，不作为可编辑框。
- 必填字段为空时弹出 `MessageBoxA` 警告。

### 4.4 查询页

查询页采用“顶部查询栏 + 下方表格”的结构。

订单查询推荐布局：

- 订单号输入框。
- 客户姓名输入框。
- 查询按钮。
- 下方订单表格。
- 底部分页信息。

刷新规则：

- 用户输入文字时，只更新输入框显示，不刷新表格。
- 点击查询按钮后，才应用查询条件并刷新结果。
- 按左右方向键翻页时刷新当前页。
- 按上下键或 Tab 切换输入控件。
- Esc 返回上级菜单。

这样可以避免输入时频繁 `cleardevice()` 导致闪烁，也符合用户“输入完成后再查询”的预期。

### 4.5 表格页

表格应使用统一表头、网格线、分页规则：

- 表头浅灰底。
- 当前行高亮使用浅青色。
- 每页默认 5 条，空间不足的查询页可显示 4 条。
- 空状态显示“暂无数据”或“暂无匹配记录”。
- 底部提示显示键盘操作：`↑↓:选择行  ←→:翻页  Enter:确认  Esc:返回`。

表格列宽应根据业务优先级分配：

- 订单号列最宽。
- 状态列较短。
- 电话、客户、货物名称保持中等宽度。
- 不要让按钮或文字越出白色面板。

## 5. 交互规范

### 5.1 键盘行为

全系统应统一支持：

- `↑ / ↓`：切换控件焦点或表格行。
- `← / →`：在分页表格中翻页。
- `Enter`：确认当前按钮或选中当前表格行。
- `Esc`：返回上级菜单。
- `Tab`：在输入控件之间切换。
- `Backspace`：删除输入框内容。

通用控件驱动 `window_run()` 必须处理 `Esc`，返回 `current = -1`，各业务页面收到后返回上级。

### 5.2 鼠标行为

- 点击输入框切换焦点。
- 点击按钮立即执行对应动作。
- 点击表格行选中该行。
- 点击分页热区翻页。

鼠标和键盘应保持等价，不应出现“鼠标能返回、键盘不能返回”的情况。

### 5.3 刷新机制

界面刷新应遵循“必要刷新”原则：

- 页面首次进入时刷新。
- 查询按钮触发时刷新。
- 翻页触发时刷新。
- 操作成功后刷新相关列表。
- 单纯输入字符时只局部重绘输入框。

避免每个键盘消息都全屏清空重画，减少闪烁和误刷新。

### 5.4 提示与错误

提示分为三类：

- 信息提示：操作成功、暂无数据，使用 `MB_ICONINFORMATION`。
- 警告提示：输入为空、状态不允许，使用 `MB_ICONWARNING`。
- 危险确认：删除、驳回、异常上报等，使用红色按钮或二次确认。

提示文字应短而明确，例如：

- 请输入订单号
- 该订单不在待审核状态
- 驳回必须填写原因
- 订单不存在

## 6. 页面设计方案

### 6.1 启动页

目标：让用户快速进入登录、找回密码或退出系统。

布局：

- 居中白色面板。
- 顶部显示“智能物流管理系统”。
- 中部显示日期。
- 纵向排列三个按钮：登录、忘记密码、退出系统。
- 底部显示“请选择操作”，与按钮保持间距。

注意：

- 白框必须居中。
- 按钮与提示文字不能重合。
- 三个按钮尺寸一致。

### 6.2 登录页

目标：完成身份认证。

布局：

- 居中白色面板。
- 标题为“智能物流管理系统登录界面”。
- 输入账号、密码。
- 密码支持隐藏/显示切换。
- 底部放置登录、返回或退出按钮。

交互：

- 空账号或空密码弹窗警告。
- 登录失败提示原因。
- 登录成功进入主菜单。

### 6.3 主菜单页

目标：根据角色展示可用模块。

布局：

- 使用统一业务面板。
- 顶部显示当前用户、角色、登录时间。
- 中间显示模块按钮。
- 不具备权限的模块不显示或显示禁用态。

模块建议：

- 系统管理
- 订单管理
- 仓储管理
- 运输管理
- 统计分析
- 客户服务
- 系统配置
- 退出系统

### 6.4 订单管理页

目标：承接订单创建、审核、查询、跟踪。

建议菜单：

- 创建订单
- 订单审核
- 订单查询
- 订单修改
- 订单跟踪
- 返回

其中“订单查询”和“订单审核”不宜合并成含糊按钮，当前代码中如出现“订单查询与审核”，建议拆成两个明确入口。

### 6.5 订单查询页

目标：按订单号、客户姓名查询订单。

布局：

- 标题：智能物流管理系统订单查询界面。
- 元信息：当前用户、角色、登录时间。
- 查询栏：订单号、客户姓名、查询按钮。
- 表格列：订单号、客户姓名、联系电话、订单状态。
- 底部：上一页、总页数、当前页、下一页。

关键交互：

- 输入时不刷新结果。
- 点击查询后应用条件。
- 左右键翻页。
- 上下键切换输入框。
- Esc 返回。

### 6.6 订单审核页

目标：管理员审核待审核订单。

推荐流程：

1. 进入页面后显示待审核订单列表。
2. 用户可用上下键选择订单，Enter 确认，Esc 返回。
3. 下方或弹窗表单填写审核操作。
4. 审核通过：订单状态变为“待出库”。
5. 驳回：必须填写驳回原因，订单状态变为“已驳回”。

当前代码中审核页采用“先显示列表，再输入订单号”的方式，也可以保留，但应保证：

- Esc 在列表层和表单层都能返回。
- 审核通过和驳回按钮位置清晰。
- 驳回按钮使用红色。
- 审核成功后刷新待审核列表。

### 6.7 订单创建页

目标：客服或管理员创建物流订单。

字段：

- 订单号：系统自动生成。
- 客户姓名。
- 联系电话。
- 客户地址。
- 货物名称。
- 货物类型。
- 货物重量。
- 货物数量。
- 货物体积。
- 发货地址。
- 收货地址。

交互：

- 必填项为空时禁止提交。
- 数量必须为正整数。
- 创建成功后提示订单号和状态。
- 初始状态为“待审核”。

### 6.8 订单跟踪页

目标：查询订单完整流程。

布局：

- 输入订单号。
- 按钮：查询订单详情、查看出入库记录、查看操作日志、出入库流水、返回。

结果展示：

- 订单基本信息。
- 当前状态。
- 驳回原因。
- 出入库记录。
- 操作日志。

### 6.9 仓储管理页

目标：处理入库、出库、库存查询、盘点。

建议菜单：

- 入库管理。
- 出库管理。
- 库存查询。
- 库存盘点。
- 返回。

入库/出库页面：

- 输入订单号。
- 可查看待处理订单。
- 可查看库存。
- 操作成功后弹窗提示，并更新订单状态。

库存查询页：

- 查询栏：货位名称、货位编号、查询按钮。
- 表格列：序号、货物名称、货物类型、库存数量、存储货位、入库时间。
- 刷新机制应与订单查询一致。

### 6.10 运输管理页

目标：创建调度单、更新运输状态、查看轨迹。

建议页面：

- 调度创建。
- 运输轨迹更新。
- 异常上报。
- 调度单查询。

状态流转：

- 待运输。
- 运输中。
- 已送达。
- 异常。
- 已完成。

异常上报必须填写原因。

### 6.11 统计分析页

目标：展示订单和运输的汇总数据。

建议内容：

- 订单总数。
- 完成率。
- 驳回率。
- 按货物类型统计。
- 按订单状态统计。
- TXT 报表导出。

由于 EasyX 图表能力有限，可以优先使用表格 + 数字摘要，不强行实现复杂图表。

## 7. 组件规范

### 7.1 按钮

- 普通按钮：蓝色选中态，白色或浅色未选中态。
- 危险按钮：红色选中态，浅红未选中态。
- 按钮文字居中。
- 同一页面按钮宽高一致。

### 7.2 输入框

- 默认白底灰边。
- 聚焦时使用蓝色边框。
- 输入内容左侧保留 5px padding。
- 字符过长时限制输入长度，避免文字溢出。

### 7.3 表格

- 表头浅灰底。
- 单元格白底。
- 使用灰色网格线。
- 选中行使用浅青底。
- 数字、状态、短字段不应占用过宽列。

### 7.4 弹窗

弹窗只用于结果反馈、错误提示和关键确认，不用于承载大量详情。大量详情应优先放在表格或详情页面中。

## 8. 代码落地建议

### 8.1 统一常量

所有颜色、字体、尺寸、间距应继续集中在：

```cpp
backup/public/ui_config.h
```

禁止在业务窗口里随意散落新的颜色值和窗口尺寸。确需新增时，应命名为语义化常量。

### 8.2 统一控件行为

`window_run()` 应作为普通表单和菜单页的统一输入驱动，持续支持：

- Enter 确认。
- Esc 返回。
- 上下键切换。
- 输入框编辑。
- 下拉框选择。

查询类页面如需局部刷新，可以手写事件循环，但必须补齐通用键盘行为。

### 8.3 消除死代码

`searchOrderWin()` 中已有新版查询实现后，旧版 `WINDOW_T win` 代码被 `return` 跳过，后续应清理，避免维护时误改无效代码。

### 8.4 查询页抽象

当前用户查询、订单查询、库存查询存在相似结构，建议后续抽象为：

- 查询栏绘制函数。
- 文本框局部重绘函数。
- 分页文本绘制函数。
- 查询提交状态与编辑状态分离机制。

这样可以避免订单查询修复过的刷新问题在库存查询中重复出现。

## 9. 验收清单

每个页面交付前应检查：

- 白色面板是否居中或符合业务布局。
- 控件是否越出白框。
- 按钮和提示文字是否重合。
- 输入框文字是否可见。
- 表格列是否截断严重。
- 上下键是否能切换控件或行。
- 左右键是否能翻页。
- Enter 是否能确认。
- Esc 是否能返回上级。
- 查询页是否只在点击查询或翻页时刷新结果。
- 空数据是否有提示。
- 错误输入是否有弹窗。
- 操作成功后是否刷新相关数据。

## 10. 当前优先优化项

结合现有界面和最近修复记录，建议优先处理：

1. 统一所有查询页的刷新机制，避免输入即刷新。
2. 统一所有表单页的 Esc 返回逻辑。
3. 检查所有白框内控件是否越界或重合。
4. 清理订单查询中的旧版死代码。
5. 将“订单查询与审核”拆分为更明确的菜单入口。
6. 将库存查询页的按钮位置收回面板内，保持与订单查询一致。
7. 为订单审核提供“选中表格行后自动填入订单号”的增强体验。

